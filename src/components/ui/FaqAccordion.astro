---
// FAQ accordion component with original styling
---

<div class="max-w-[800px] mx-auto">
  <!-- FAQ Item 1 -->
  <div class="border-b border-gray-200">
    <button 
      class="faq-button py-8 w-full text-left cursor-pointer flex justify-between items-center gap-8 group"
      aria-expanded="false"
      aria-controls="faq-1"
    >
      <span class="text-[20px] font-heading font-normal text-gray-900 leading-relaxed">
        Do you work on systems you didn't install?
      </span>
      <div class="flex-shrink-0 w-8 h-8 rounded-lg bg-gray-100 flex items-center justify-center transition-colors group-hover:bg-gray-200">
        <div class="relative w-4 h-4">
          <!-- Horizontal line - always visible -->
          <div class="absolute top-1/2 left-0 w-full h-0.5 bg-gray-600 transform -translate-y-1/2"></div>
          <!-- Vertical line - rotates to horizontal when open -->
          <div class="faq-icon absolute top-0 left-1/2 w-0.5 h-full bg-gray-600 transform -translate-x-1/2 transition-transform duration-300"></div>
        </div>
      </div>
    </button>
    <div 
      id="faq-1" 
      class="faq-content overflow-hidden transition-all duration-500 ease-in-out max-h-0 opacity-0"
    >
      <div class="pb-8 text-gray-600 text-[14px] leading-[1.5] font-heading max-w-3xl">
        Yes! We repair all solar systems regardless of who installed them. Our technicians are trained on all major brands and can work with any system configuration. We'll ensure all repairs maintain your existing warranties.
      </div>
    </div>
  </div>

  <!-- FAQ Item 2 -->
  <div class="border-b border-gray-200">
    <button 
      class="faq-button py-8 w-full text-left cursor-pointer flex justify-between items-center gap-8 group"
      aria-expanded="false"
      aria-controls="faq-2"
    >
      <span class="text-[20px] font-heading font-normal text-gray-900 leading-relaxed">
        Will repairs void panel/inverter warranties?
      </span>
      <div class="flex-shrink-0 w-8 h-8 rounded-lg bg-gray-100 flex items-center justify-center transition-colors group-hover:bg-gray-200">
        <div class="relative w-4 h-4">
          <div class="absolute top-1/2 left-0 w-full h-0.5 bg-gray-600 transform -translate-y-1/2"></div>
          <div class="faq-icon absolute top-0 left-1/2 w-0.5 h-full bg-gray-600 transform -translate-x-1/2 transition-transform duration-300"></div>
        </div>
      </div>
    </button>
    <div 
      id="faq-2" 
      class="faq-content overflow-hidden transition-all duration-500 ease-in-out max-h-0 opacity-0"
    >
      <div class="pb-8 text-gray-600 text-[14px] leading-[1.5] font-heading max-w-3xl">
        No, our repairs are designed to maintain all existing warranties. We use manufacturer-approved parts and follow all warranty guidelines. In fact, proper maintenance and repairs can help extend the life of your system.
      </div>
    </div>
  </div>

  <!-- FAQ Item 3 -->
  <div class="border-b border-gray-200">
    <button 
      class="faq-button py-8 w-full text-left cursor-pointer flex justify-between items-center gap-8 group"
      aria-expanded="false"
      aria-controls="faq-3"
    >
      <span class="text-[20px] font-heading font-normal text-gray-900 leading-relaxed">
        How long does a typical repair take?
      </span>
      <div class="flex-shrink-0 w-8 h-8 rounded-lg bg-gray-100 flex items-center justify-center transition-colors group-hover:bg-gray-200">
        <div class="relative w-4 h-4">
          <div class="absolute top-1/2 left-0 w-full h-0.5 bg-gray-600 transform -translate-y-1/2"></div>
          <div class="faq-icon absolute top-0 left-1/2 w-0.5 h-full bg-gray-600 transform -translate-x-1/2 transition-transform duration-300"></div>
        </div>
      </div>
    </button>
    <div 
      id="faq-3" 
      class="faq-content overflow-hidden transition-all duration-500 ease-in-out max-h-0 opacity-0"
    >
      <div class="pb-8 text-gray-600 text-[14px] leading-[1.5] font-heading max-w-3xl">
        Most repairs are completed within 1-2 days. Simple fixes (like resetting an inverter or replacing a fuse) can be done same-day. Complex repairs requiring special ordered parts may take up to 2-3 weeks.
      </div>
    </div>
  </div>

  <!-- FAQ Item 4 -->
  <div class="border-b border-gray-200">
    <button 
      class="faq-button py-8 w-full text-left cursor-pointer flex justify-between items-center gap-8 group"
      aria-expanded="false"
      aria-controls="faq-4"
    >
      <span class="text-[20px] font-heading font-normal text-gray-900 leading-relaxed">
        What if you can't find the problem?
      </span>
      <div class="flex-shrink-0 w-8 h-8 rounded-lg bg-gray-100 flex items-center justify-center transition-colors group-hover:bg-gray-200">
        <div class="relative w-4 h-4">
          <div class="absolute top-1/2 left-0 w-full h-0.5 bg-gray-600 transform -translate-y-1/2"></div>
          <div class="faq-icon absolute top-0 left-1/2 w-0.5 h-full bg-gray-600 transform -translate-x-1/2 transition-transform duration-300"></div>
        </div>
      </div>
    </button>
    <div 
      id="faq-4" 
      class="faq-content overflow-hidden transition-all duration-500 ease-in-out max-h-0 opacity-0"
    >
      <div class="pb-8 text-gray-600 text-[14px] leading-[1.5] font-heading max-w-3xl">
        If our diagnostic doesn't identify an issue, you only pay the reduced diagnostic fee of $99 instead of the full $149-$249. We're confident in our ability to pinpoint problems—but we don't charge full price if we can't help.
      </div>
    </div>
  </div>

  <!-- FAQ Item 5 -->
  <div class="border-b border-gray-200">
    <button 
      class="faq-button py-8 w-full text-left cursor-pointer flex justify-between items-center gap-8 group"
      aria-expanded="false"
      aria-controls="faq-5"
    >
      <span class="text-[20px] font-heading font-normal text-gray-900 leading-relaxed">
        Do you offer emergency service?
      </span>
      <div class="flex-shrink-0 w-8 h-8 rounded-lg bg-gray-100 flex items-center justify-center transition-colors group-hover:bg-gray-200">
        <div class="relative w-4 h-4">
          <div class="absolute top-1/2 left-0 w-full h-0.5 bg-gray-600 transform -translate-y-1/2"></div>
          <div class="faq-icon absolute top-0 left-1/2 w-0.5 h-full bg-gray-600 transform -translate-x-1/2 transition-transform duration-300"></div>
        </div>
      </div>
    </button>
    <div 
      id="faq-5" 
      class="faq-content overflow-hidden transition-all duration-500 ease-in-out max-h-0 opacity-0"
    >
      <div class="pb-8 text-gray-600 text-[14px] leading-[1.5] font-heading max-w-3xl">
        Yes! For systems that are completely down or creating safety hazards, we offer priority emergency service. Call us at (385) 539-8892 and mention "emergency" to get scheduled within 24 hours.
      </div>
    </div>
  </div>

</div>

<script is:inline>
  // FAQ Accordion functionality with original styling
  document.addEventListener('DOMContentLoaded', function() {
    const faqButtons = document.querySelectorAll('.faq-button');
    
    faqButtons.forEach(button => {
      button.addEventListener('click', function() {
        const controlsId = this.getAttribute('aria-controls');
        if (!controlsId) return;
        
        const content = document.getElementById(controlsId);
        if (!content) return;
        
        const isExpanded = this.getAttribute('aria-expanded') === 'true';
        const icon = this.querySelector('.faq-icon');
        
        // Close all other FAQs
        faqButtons.forEach(otherButton => {
          if (otherButton !== this) {
            const otherControlsId = otherButton.getAttribute('aria-controls');
            if (!otherControlsId) return;
            
            const otherContent = document.getElementById(otherControlsId);
            const otherIcon = otherButton.querySelector('.faq-icon');
            otherButton.setAttribute('aria-expanded', 'false');
            
            if (otherContent) {
              otherContent.classList.remove('max-h-96', 'opacity-100');
              otherContent.classList.add('max-h-0', 'opacity-0');
            }
            if (otherIcon) {
              otherIcon.classList.remove('rotate-90');
            }
          }
        });
        
        // Toggle current FAQ
        if (isExpanded) {
          this.setAttribute('aria-expanded', 'false');
          content.classList.remove('max-h-96', 'opacity-100');
          content.classList.add('max-h-0', 'opacity-0');
          if (icon) icon.classList.remove('rotate-90');
        } else {
          this.setAttribute('aria-expanded', 'true');
          content.classList.remove('max-h-0', 'opacity-0');
          content.classList.add('max-h-96', 'opacity-100');
          if (icon) icon.classList.add('rotate-90');
        }
      });
      
      // Keyboard navigation
      button.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          this.click();
        }
      });
    });
  });
</script>